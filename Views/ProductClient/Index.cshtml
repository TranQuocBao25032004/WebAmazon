@model  List<WebAmazon.Models.ProDetail>
@*@using WebAmazon.Models;
@using PagedList.Mvc;
@model PagedList.PagedList<WebAmazon.Models.ProDetail>*@

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_HeadPhoneLayout.cshtml";
}

<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<script src="~/Scripts/bootstrap.min.js"></script>
<style>
    

    .box1 {
        height: 600px;
        
        background-color: white;
        padding: 20px 0px 15px;
        margin-top: 15px;
    }
    .hang{
        width:max-content;
    }
</style>
<h2>Results</h2>
<div class="container-fluid">
    <div class="hang">
        @foreach (var pro in Model)
        {
            <div class="box1">
                <div class="hinhanh">
                    <img src="@Url.Content(pro.Product.ProImage)" height="300" width="300" />
                </div>
                <div class="div-des-client">
                    <h3>@Html.DisplayFor(model => pro.Product.ProName)</h3>
                    <p>@Html.DisplayFor(model => pro.Price)</p>
                    <p>@Html.DisplayFor(model => pro.Color.ColorName)</p>
                    <a href="/ShoppingCart/AddToCart/@pro.ProID" class="btn btn-outline-secondary">Buy</a>
                </div>

            </div>


        }
    </div>

</div>@*
Trang @(Model.PageCount<Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
<div class="MenuPage">
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))
</div>*@

